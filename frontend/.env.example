# Frontend env (Vite). Copy to .env and fill in as needed.
# Currently the frontend uses NO env vars, so this is optional.
# Vite only exposes vars prefixed with VITE_ to the client.

# Base URL of the backend API (used once the backend is wired up).
VITE_API_URL=http://localhost:8000
