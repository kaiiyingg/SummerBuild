# Backend env. Copy to .env and fill in.
# No backend server exists yet (only backend/db/table.sql), so these are
# placeholders for when the API/database is implemented.

# Database connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=summerbuild
DB_USER=postgres
DB_PASSWORD=changeme

# API server
PORT=8000
